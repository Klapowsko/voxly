services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    volumes:
      - ./data:/data:z
      - ./backend:/app:z
    environment:
      - APP_API_TOKEN=${APP_API_TOKEN:-dev-token}
      - APP_WHISPER_MODEL=${APP_WHISPER_MODEL:-base}
      - APP_WHISPER_DEVICE=${APP_WHISPER_DEVICE:-auto}
      - APP_SPELLBOOK_URL=${APP_SPELLBOOK_URL:-https://spellbook-api.klapowsko.com}
      - APP_DATA_DIR=/data
    env_file:
      - ./backend/.env
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3002:3000"
    volumes:
      - ./frontend:/app:z
      - frontend_node_modules:/app/node_modules
      - frontend_next:/app/.next
    env_file:
      - ./frontend/.env
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  frontend_node_modules:
  frontend_next:

